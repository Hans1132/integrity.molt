# Public URL — default, works for Claude Desktop / Codex CLI users
INTEGRITY_MOLT_BASE_URL=https://intmolt.org

# Override for VPS-local use (when MCP server runs on the same machine as the backend)
# INTEGRITY_MOLT_BASE_URL=http://127.0.0.1:3402
